<nav id="responsive-header" class="navbar-dark bg-dark">
    <div class="justify-content-end" id="navbarNav">
        <ul class="nav justify-content-around">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('home') }}"><i class="fa fa-home"></i></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('home') }}#tricks_section"><i class="fa fa-bookmark"></i></a>
            </li>
            {% if app.user is not null %}
                {% if app.user.activated == true %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('logout') }}"><i class="fa fa-sign-out"></i></a>
                    </li>
                {% endif %}
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('login') }}"><i class="fa fa-sign-in"></i></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('sign_up') }}"><i class="fa fa-user-plus"></i></a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
