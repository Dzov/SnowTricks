{% extends('base.html.twig') %}

{% block body %}
    <section class="login__section d-flex flex-column align-items-center justify-content-center">
        <h3 class="mb-5">Se connecter</h3>
        {{ form_start(form, {'attr': {'class': 'forms login__form d-flex flex-column'}}) }}
        <div class="form-group">
            {{ form_row(form.email) }}
        </div>
        <div class="form-group">
            {{ form_row(form.password) }}
        </div>
        <div class="form-group text-center">
            <label class="form-check-label"><a href="{{ path('forgot_password') }}">Mot de passe oublié ?</a></label>
        </div>
        <button type="submit" class="btn btn-primary">Valider</button>
        {{ form_end(form) }}
    </section>
{% endblock %}
